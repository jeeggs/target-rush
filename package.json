{
  "name": "target-rush-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Target Rush for Android",
  "scripts": {
    "start": "python3 serve.py",
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "build:android": "npm run sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
