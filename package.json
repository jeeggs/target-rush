{
  "name": "target-rush-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Target Rush for iPhone and Android",
  "scripts": {
    "start": "python3 serve.py",
    "sync": "npx cap sync",
    "ios": "npx cap open ios",
    "android": "npx cap open android",
    "build:ios": "npm run sync && npx cap open ios",
    "build:android": "npm run sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
